# Инструкция по интеграции ЮKassa

## 📋 Что подготовлено

### 1. **Серверная часть** (`payment_server.py`)
- Создание платежей через API ЮKassa
- Проверка статуса платежей
- Webhook для уведомлений от ЮKassa
- CORS для взаимодействия с фронтендом

### 2. **Клиентская часть**
- `src/payment.js` — сервис работы с платежами
- `src/payment_handler.js` — обработчик кнопки оплаты
- Автоматическая разблокировка контента после оплаты

### 3. **Конфигурация**
- `requirements.txt` — зависимости Python
- `.env.example` — шаблон переменных окружения

---

## 🚀 Шаги по настройке

### Шаг 1: Установка зависимостей

```bash
cd "c:\Users\Admin\Desktop\матрица судьбы"
pip install -r requirements.txt
```

### Шаг 2: Настройка переменных окружения

1. Скопируй `.env.example` в `.env`:
```bash
copy .env.example .env
```

2. Открой `.env` и добавь свои данные ЮKassa:
```
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
```

### Шаг 3: Настройка webhook в ЮKassa

1. Войди в личный кабинет ЮKassa
2. Перейди в "Настройки" → "Уведомления"
3. Укажи URL вебхука: `https://ваш-домен.site/webhook`
4. Сохрани настройки

### Шаг 4: Обновление URL сервера

В файле `src/payment.js` замени:
```javascript
serverUrl: 'http://localhost:5000'
```
на:
```javascript
serverUrl: 'https://ваш-домен.site'
```

### Шаг 5: Запуск сервера

**Для тестирования локально:**
```bash
python payment_server.py
```

**Для продакшн (с gunicorn):**
```bash
gunicorn -w 4 -b 0.0.0.0:5000 payment_server:app
```

---

## 🧪 Тестирование

### Тестовые данные ЮKassa

**Тестовая карта для успешной оплаты:**
- Номер: `5555 5555 5555 4444`
- Срок действия: любая дата из будущего
- CVC: любой трёхзначный код
- Имя: любое

**Другие тестовые карты:**
- `4111 1111 1111 1111` — успешный платёж
- `5555 5555 5555 5557` — отклоненный платёж

### Проверка работы

1. Открой сайт в браузере
2. Введи данные для расчета матрицы
3. Нажми кнопку "Получить полный расклад — 590 ₽"
4. Произведи тестовую оплату
5. Проверь разблокировку контента после возврата

---

## 📁 Структура файлов

```
матрица судьбы/
├── payment_server.py          # Сервер обработки платежей
├── requirements.txt           # Зависимости Python
├── .env                       # Секретные ключи (НЕ коммитить!)
├── .env.example              # Пример конфигурации
├── src/
│   ├── payment.js            # Клиентский сервис платежей
│   └── payment_handler.js    # Обработчик кнопки оплаты
├── index.html                # Подключены скрипты оплаты
└── compatibility.html        # Подключены скрипты оплаты
```

---

## 🔒 Безопасность

### ⚠️ ВАЖНО:

1. **Никогда** не добавляй файл `.env` в git
2. Храни секретные ключи только на сервере
3. Используй HTTPS для продакшн-сервера
4. Проверяй подпись webhook от ЮKassa (опционально)

### Добавь в .gitignore:
```
.env
*.pyc
__pycache__/
```

---

## 📊 Логика работы

### 1. Пользователь нажимает кнопку оплаты

```
Клиент → payment_handler.js → Собирает данные пользователя
                              → Отправляет запрос на сервер
```

### 2. Сервер создает платёж

```
payment_server.py → Создает запрос к ЮKassa API
                  → Получает confirmation_url
                  → Возвращает клиенту
```

### 3. Перенаправление на оплату

```
Клиент → Перенаправляется на страницу ЮKassa
       → Пользователь вводит данные карты
       → Оплачивает
```

### 4. Возврат после оплаты

```
ЮKassa → Возвращает на return_url
       → payment.js проверяет статус
       → Разблокирует контент
       → Сохраняет доступ в localStorage
```

### 5. Уведомление от ЮKassa (webhook)

```
ЮKassa → Отправляет POST на /webhook
       → Сервер обрабатывает уведомление
       → Можно сохранить в БД / отправить email
```

---

## 🎯 Цены услуг

Текущие цены (можно изменить в `payment_server.py`):

```python
PRICES = {
    'personal': {
        'amount': '590.00',
        'description': 'Полный расклад Матрицы Судьбы'
    },
    'compatibility': {
        'amount': '590.00',
        'description': 'Матрица совместимости'
    }
}
```

---

## 🐛 Отладка

### Проверка здоровья сервера:
```bash
curl http://localhost:5000/health
```

### Логи сервера:
```bash
tail -f /var/log/payment_server.log
```

### Проверка статуса платежа вручную:
```bash
curl http://localhost:5000/check-payment/PAYMENT_ID
```

---

## 📞 Поддержка

Если возникнут вопросы:
1. Проверь логи сервера
2. Проверь консоль браузера (F12)
3. Убедись что API ключи правильные
4. Проверь документацию ЮKassa: https://yookassa.ru/developers

---

## ✅ Чеклист перед запуском

- [ ] Установлены зависимости Python
- [ ] Создан файл `.env` с ключами
- [ ] Настроен webhook в ЮKassa
- [ ] Обновлен URL сервера в `payment.js`
- [ ] Протестирована оплата с тестовой картой
- [ ] Работает разблокировка контента
- [ ] Сервер работает на продакшн-домене
- [ ] Настроен HTTPS

---

## 🎉 Готово!

После выполнения всех шагов система приема платежей будет полностью готова к работе!
