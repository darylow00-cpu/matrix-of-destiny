# Инструкция по деплою Cloudflare Worker для оплаты

## Шаг 1: Создай аккаунт на Cloudflare

1. Открой https://workers.cloudflare.com/
2. Нажми "Sign Up" (бесплатно, не требует карту)
3. Подтверди email

## Шаг 2: Создай новый Worker

1. В дашборде нажми "Create a Service" (или "Create Worker")
2. Название: `matrix-payment` (или любое другое)
3. Выбери "HTTP Handler"
4. Нажми "Create Service"

## Шаг 3: Скопируй код

1. В редакторе Worker удали весь код по умолчанию
2. Открой файл `cloudflare-worker.js` из репозитория
3. Скопируй весь код и вставь в редактор Worker
4. Замени на свои данные (строки 7-8):
   ```javascript
   const SHOP_ID = '383745'; // Твой SHOP_ID от ЮKassa
   const SECRET_KEY = 'test_AG7eOF4eR-_4xGgvAiuGg6WfQY4qmv2B73TRN0xRoHQ'; // Твой SECRET_KEY
   ```

## Шаг 4: Сохрани и задеплой

1. Нажми "Save and Deploy"
2. Worker развернётся за несколько секунд
3. Получишь URL типа: `https://matrix-payment.YOUR-SUBDOMAIN.workers.dev`

## Шаг 5: (Опционально) Добавь Environment Variables

Вместо хардкода SHOP_ID и SECRET_KEY можно использовать переменные окружения:

1. В настройках Worker → Settings → Variables
2. Добавь:
   - `YOOKASSA_SHOP_ID` = `383745`
   - `YOOKASSA_SECRET_KEY` = `test_AG7eOF4eR-_4xGgvAiuGg6WfQY4qmv2B73TRN0xRoHQ`
3. Нажми "Encrypt" для SECRET_KEY
4. Сохрани

## Шаг 6: Обнови код сайта

1. Открой файл `src/payment.js`
2. Замени URL в строке с `serverUrl`:
   ```javascript
   serverUrl: (function() {
       const isLocal = ['localhost', '127.0.0.1'].includes(window.location.hostname);
       return isLocal ? 'http://127.0.0.1:5000' : 'https://matrix-payment.YOUR-SUBDOMAIN.workers.dev';
   })(),
   ```
3. Замени `YOUR-SUBDOMAIN` на свой поддомен из шага 4

## Шаг 7: Проверь работу

1. Открой в браузере: `https://matrix-payment.YOUR-SUBDOMAIN.workers.dev/health`
2. Должен вернуть JSON:
   ```json
   {"status":"ok","timestamp":"2025-12-16T..."}
   ```
3. Если работает — всё готово!

## Шаг 8: Опубликуй изменения на GitHub

```powershell
git add .
git commit -m "Add Cloudflare Worker for payments"
git push
```

## Готово! 🎉

Теперь:
- Открой свой сайт: https://www.xn--80aaxovl4a.site
- Нажми "Получить полный расклад — 590 ₽"
- Тебя перенаправит на страницу оплаты ЮKassa

---

## Troubleshooting

### Ошибка 405
- Проверь, что Worker развёрнут и доступен по URL
- Убедись, что в `src/payment.js` правильный URL Worker

### CORS ошибка
- Worker уже настроен на CORS, но если возникла проблема — проверь, что origin добавлен в corsHeaders

### "Сервер вернул не JSON"
- Открой URL Worker в браузере и проверь `/health`
- Если видишь HTML вместо JSON — значит, URL неправильный или Worker не развёрнут

### Платёж не создаётся
- Проверь SHOP_ID и SECRET_KEY
- Убедись, что у ЮKassa активен тестовый режим
- Посмотри логи в Cloudflare (Workers → Logs)
